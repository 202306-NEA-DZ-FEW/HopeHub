import renderer from 'react-test-renderer';
import BlogsCarousel from '../BlogsCarousel';

import "./matchMedia.mock";

it('renders correctly', () => {
  const tree = renderer
    .create(<BlogsCarousel />)
    .toJSON();
  expect(tree).toMatchSnapshot();
});